import { defineComponent } from 'vue'; import { props } from './props';
import'./<%- camelizeName %>.<%- styleCss %>'; export default defineComponent({
name: '<%- bigCamelizeName %>', props, setup(props, { slots }) { return () => {
return (
<div class="<%- kebabCaseName %>">{ slots.default?.() }</div>
) } }, })
